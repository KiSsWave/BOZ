import 'package:flutter/material.dart';

class CheckBillPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Check Bill'),
      ),
      body: Center(
        child: Text('Check Bill Page'),
      ),
    );
  }
}